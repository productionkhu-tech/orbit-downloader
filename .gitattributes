# Windows batch files MUST be CRLF — cmd.exe silently exits on LF endings.
# Mark as binary so git never normalizes them away from CP949 + CRLF.
*.bat -text
install.template.bat text eol=lf

# Other text files can use auto-detected line endings.
* text=auto
